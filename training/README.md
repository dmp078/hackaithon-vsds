# Training Placeholder

This directory is intentionally empty for now.

Do not place `data/gold_validation.json` here. The curated gold set is for evaluation only and must not be used for fine-tuning.
